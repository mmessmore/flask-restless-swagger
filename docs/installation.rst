============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install flask-restless-swagger
    $ pip install flask-restless-swagger

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-restless-swagger
    $ pip install flask-restless-swagger
